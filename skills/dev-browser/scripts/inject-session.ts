// Inject full session from Cookie Bridge into browser context
// Fast path: scripts/inject-session.sh handles this via curl (no tsx needed)
console.error("inject-session should use the .sh fast path");
process.exit(99);

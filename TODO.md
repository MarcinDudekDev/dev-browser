# Port over helper methods from do browser:

[] There should be a method that gets the a11y tree of the current page with helpful ids
[] There should be a method that given a helpful id gets the element to interact with.
